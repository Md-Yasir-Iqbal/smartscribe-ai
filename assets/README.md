# Assets

Put screenshots referenced from the main `README.md` in this folder, e.g.:

- `home.png` — the landing page
- `text-workspace.png` — the Summarize Text workspace
- `pdf-workspace.png` — the Summarize PDF workspace
- `result.png` — a generated result with Document Insights

These are referenced from the root `README.md` as placeholders — add your
own screenshots after running the app locally.
